{
  "name": "logseq-interface",
  "version": "0.6.0",
  "description": "Agentic access to Ethan's Logseq graph via the logseq CLI - defers to the CLI's self-documentation, and adds the boundary, graph semantics, and graph conventions it does not cover.",
  "author": {
    "name": "Ethan Porter"
  },
  "userConfig": {
    "default_graph": {
      "type": "string",
      "title": "Default Logseq graph",
      "description": "Name of the Logseq graph to use by default, without prompting for discovery.",
      "required": true
    }
  }
}
