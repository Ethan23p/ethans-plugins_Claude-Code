# logseq-mcp

Connects Claude to the [Logseq](https://logseq.com) DB graph you have open in the desktop app.

## Prerequisites

- Logseq desktop running with **HTTP APIs server** enabled (Settings → Features) — this starts the MCP server at `127.0.0.1:12315`.

## Install

```
/plugin install logseq-mcp
```
