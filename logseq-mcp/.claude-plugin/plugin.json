{
  "name": "logseq-mcp",
  "version": "0.1.5",
  "description": "Wires Logseq's bundled MCP server into Claude Code. Talks to the running Logseq desktop app via its local HTTP API server, so the agent and the editor share the same live graph.",
  "author": {
    "name": "Ethan"
  },
  "mcpServers": {
    "logseq": {
      "url": "http://127.0.0.1:12315/mcp"
    }
  }
}
